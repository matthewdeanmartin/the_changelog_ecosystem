# dotnet-releaser configuration seed for offline/local experiment.
# Uses the format generated by `dotnet-releaser new`.
# GitHub credentials are absent on purpose — this lets us probe which
# sub-commands work without a live remote.

[msbuild]
project = "TipCalc.csproj"

[github]
user = "test"
repo = "test"
